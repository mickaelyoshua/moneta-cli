[package]
name = "moneta-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = {version = "0.9.0", features = ["runtime-tokio", "tls-native-tls", "postgres", "macros", "chrono"]}
tokio = {version = "1.52.3", features = ["full"]}
